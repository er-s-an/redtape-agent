# Recorded genuine Strands demonstration

The September 12, 2026 submission film is 128 seconds, in English. It shows a genuine hosted Strands model selecting tools against the shipped synthetic Xiao case and government sandbox. Requested and returned model ID: kimi-for-coding. SDK: Strands Agents 1.55.1. The model, tools, planner, guard and steering files match the hashes in demo-proof.json.

The agent scans documents, reads rules, computes backward deadlines, searches slots, prepares a draft and date holds, and attempts S-1003. The deterministic guard blocks it before approval. The model eventually produces a decision with concrete slot, fee, projected return and trip margin. Synthetic operator approval covers the exact displayed S-1003 expedited option. The same persisted session and Store then resume; the model selects check_decisions, book_appointment and mark_decision_executed. The sandbox returns CNF-B1000. Hash-chain verification passes; no application is filed.

The first wake used 37 hosted requests and generated the decision. A local timer interrupted its caller; SDK worker cleanup continued before the incomplete result returned. Follow-through resumed with a focused natural-language prompt in eight more requests and ended normally. Total 45 requests, 43 completed and two steering connection failures. Three calendar-hold attempts received steering cancellation/guidance; four actual holds were written. These facts bound the proof; it is not uniformly perfect calls, an uninterrupted run or a full reliability suite. The film condenses waiting and makes no instant-response claim.

The model generated different alternatives from the original UI's hardcoded labels. The release now shows actual option labels and structured fee/tier/projection/margin, and binds receipt values to the matching approved option and returned booking. Pending images are recorded-state views through that correction; receipt images came from the same-case live local Store. Ledger excerpts use actual recorded events with larger type. Approval is a restrained graphic of the recorded exact-option choice, not a fabricated browser click.

The preapproval draft shows the regular base fee $23. The later $83 is the selected expedited processing option; this does not claim an updated filed application or payment. No real passport data, government action or user outcome was exercised. Raw local traces, credentials, account identifiers and private production paths are intentionally not included in the public proof.
